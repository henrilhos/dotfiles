# /Users/henrilhos/.config/tmuxinator/torre.yml

name: torre
root: ~/Projects/px-torre-core

windows:
  - editor:
      panes:
        - nvim .
  - claude:
      panes:
        - claude
  - server:
      panes:
        - server:
            - sail up -d --build && lazydocker
