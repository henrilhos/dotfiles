# /Users/henrilhos/.config/tmuxinator/painel.yml

name: painel
root: ~/Projects/px-painel

windows:
  - editor:
      panes:
        - editor:
            - nvim .
  - claude:
      panes:
        - claude:
            - claude
  - server:
      panes:
        - server:
            - npm run dev
        -  # empty pane
      layout: main-vertical
